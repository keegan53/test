apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  selector:
    apps: nginx
  ports:
   - name: http
     protocol: TCP
     port: 80
     targetPort: 80
    image: nginx:alpine
    ports:
    - containerPorts: 80